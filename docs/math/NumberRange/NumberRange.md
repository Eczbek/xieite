# gcufl::math::NumberRange<N>::NumberRange
Declared in `<gcufl/math/NumberRange.hpp>`
```cpp
constexpr NumberRange(const N begin, const N end) noexcept
: begin(begin), end(end) {}
```
Creates a number range between `begin` and `end`.
